[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "brokermq"
version = "0.1.0"
description = "Python SDK for broker-message MQTT broker"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["brokermq*"]
